# Copy to .env and fill in. Never commit .env.

# Required from Phase 3 (reasoning panel). Not needed for data/backtest phases.
ANTHROPIC_API_KEY=

# Optional: override data directory (defaults to ./data)
# SWING_DATA_DIR=/Users/you/swing-data
